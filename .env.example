# Environment Configuration for MobSF MCP Server
# Copy this file to .env and fill in the values

# MobSF Configuration
# URL to your MobSF instance
# Use http://host.docker.internal:9000 when running MCP server in Docker
# Use http://localhost:9000 when running locally
MOBSF_URL=http://host.docker.internal:9000

# MobSF REST API Key
# Get this from MobSF logs: docker logs mobsf | grep "REST API Key"
MOBSF_API_KEY=your-mobsf-api-key-here

# MCP Server Authentication (optional but recommended)
# Set this to require Bearer token authentication for MCP endpoints
# Generate a secure token: openssl rand -hex 32
# If not set, MCP server will run without authentication
MCP_API_KEY=

# MCP Server Port (optional, defaults to 7567)
# PORT=7567
